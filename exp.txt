Website: http://127.0.0.1:5000

View database:
    sudo -u postgres psql -d vulnerable_bank

    Commands:
        \dt                -- list tables in the current search_path (usually 'public')


Usefull commands for exploiting

SQL Injection Login:
    "admin'OR 1=1 --"

